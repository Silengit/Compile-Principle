struct OP
{
    int f;
};

int fibo(int n)
{
    if(n < 2)
        return 1;
    else
        return fibo(n-1) + fibo(n-2); 
}

int main()
{
    int i = 0, x;
    struct OP op;
    while(i < 10){
        op.f = fibo(i);
        write(op.f);
        i = i+1;
    }
    return 0;
}